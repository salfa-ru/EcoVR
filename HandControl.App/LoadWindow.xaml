<Window x:Class="HandControl.App.LoadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HandControl.App"
        mc:Ignorable="d"
        Title="LoadWindow" Height="200" Width="400" HorizontalAlignment="Center" VerticalAlignment="Center" WindowStyle="None" ResizeMode="NoResize">
    <StackPanel x:Name="stackState">
        <TextBlock Text="{Binding ProgramTitle}"/>
        <TextBlock Text="{Binding ProgramDescription}"/>
        <TextBlock Text="{Binding ProgramRights}"/>
        <TextBlock Text="{Binding State}"/>
    </StackPanel>
</Window>
